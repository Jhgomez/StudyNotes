
* public abstract class **Transition** / package androidx.transition: Subclasses are Autotransition, ChangeBounds, ChangeClipBounds, ChangeImageTransform, ChangeScroll, ChangeTransform, Explode, Fade, Hold,
  LavelMoveTransition, Slide, TextScale, TransitionSet, Visibility. Any Transition has two main jobs: (1) capture property values, and (2) play animations based on changes to captured property values. Transitions
  may not work correctly with either SurfaceView or TextureView, due to the way that these views are displayed on the screen. For SurfaceView, the problem is that the view is updated from a non-UI
  thread, so changes to the view due to transitions (such as moving and resizing the view) may be out of sync with the display inside those bounds. TextureView is more compatible with transitions in
  general, but some specific transitions (such as Fade) may not be compatible with TextureView because they rely on android.view.ViewOverlay functionality, which does not currently work
  with TextureView. Transitions can be declared in XML resource files inside the res/transition directory.

* public interface **TypeEvaluator<T>**/ package android.animation: Interface for use with the ValueAnimator.setEvaluator(TypeEvaluator) function. Evaluators allow developers to create animations on arbitrary
  property types, by allowing them to supply custom evaluators for types that are not automatically understood and used by the animation system.

* public class **ViewGroupOverlay** extends **ViewOverlay**/ package android.view: is an extra layer that sits on top of a ViewGroup (the "host view") which is drawn after all other content in that
  view (including the view group's children). Interaction with the overlay layer is done by adding and removing views and drawables. You can not instantiate this class instead a view child like ImageView 
  or TextView will call `this.getOverlay()` it return and instance of `ViewOverlay` which only accepts drawables while in a ViewGroup child LinearLayout, FrameLayout, etc we get an `ViewGroupOverlay`
  which accepts also views besides drawables

* public class **GestureOverlayView** / package android.gesture: A transparent overlay for gesture input that can be placed on top of other widgets or contain other widgets.

* public class [GestureDetector](https://developer.android.com/develop/ui/views/layout/custom-views/making-interactive#inputgesture) / package android.view: Detects various gestures and events using the supplied MotionEvents. The GestureDetector.OnGestureListener callback will notify users when a
  particular motion event has occurred. This class should only be used with MotionEvents reported via touch (don't use for trackball events). To use this class:

    * Create an instance of the GestureDetector for your View
    * In the View.onTouchEvent(MotionEvent) method ensure you call onTouchEvent(MotionEvent). The methods defined in your callback will be executed when the events occur.
    * If listening for GestureDetector.OnContextClickListener.onContextClick(MotionEvent) you must call onGenericMotionEvent(MotionEvent) in View.onGenericMotionEvent(MotionEvent).
 
  We can use this API/interface to implement things like [drag and scale](https://developer.android.com/develop/ui/views/touch-and-input/gestures/scale)

* **ColorFilter vs Tint**: You can set color filter to an ImageView, drawable and paint objects and a tint color to a drawable, they are very similar since they change the color of a drawable, you could
  even think of setting tint color property as a simple version of setting color filter, you might like to use tint property for simple use cases for that use the `setTint` method or the properties in the layout
  with `android:tint` and `android:tintMode` to set mode, but for more complex tasks you should use color filter. You can use/set color filter in any Paint object, this means you can use it to draw on the canvas   of any custom view. There is three implementations of color filter interface `PortterDuffColorFIlter`, `LightingColorFIlter` and `ColorMatrixColorFilter` being the last one the most flexible meaning it
  allows you to manipulate the colors even further. There are some alternatives to Color Filter like `Shader` and `MaskFilter`, you can not create a custom implementation of Color Filter if you
  need "full power" you can use the OpenGL alternative which would give access to GLSL(GL shader language) shaders

* **Pallete API**: API included in Android Support Library, it lets you extract prominent colors from an image. You can load your drawables as a Bitmap and pass it to Palette to access its colors. For more information, read [Selecting colors with the Palette API.](https://developer.android.com/develop/ui/views/graphics/palette-colors)

* Using Glide vs Loading Local drawable resources: Leverage caching strategies. Implement disk and memory caching to prevent redundant resource loading. Using libraries such as Glide or Picasso can help reduce memory usage by managing image loading efficiently. On average, these libraries can decrease memory consumption by 50%, leading to smoother scroll performance and a more responsive interface.

* Resource optimization

| Optimization Technique |	Percentage Improvement | Tool/Technology |
| ---------------------- | ----------------------- | --------------- |
| Vector Graphics	| 80% file size reduction	| SVG |
| Adaptive Icons	| 20% loading time decrease	| Android Adaptive SDK |
| Image Compression	| 25-34% size savings	| WebP |
| Memory Caching	| 50% memory usage reduction	| Glide/Picasso |
| Lazy Loading	| 30% load time reduction	| Custom Implementation |
| Resolution-Specific Assets | Variable improvement	| Android Resource Qualifiers |
| Unused Asset Cleanup	| 5-15% APK size reduction	| Android Lint |

* Types of Drawables: there are several but in this case we want to mention "Nine-Patch"/"9-Patch" Images which are usually used for Stretchable Backgrounds

* **9-Patch vs Vector**: In vector graphics all sides are scaled or stretched when we set it to any background whereas in 9-patch we can define which sides can scale or stretch so at runtime only those side scale
  which we set it to scale in 9-patch tool.

* **Shader**: They are applied to a paint objcet. Implementations we can use are:
    * **BitmapShader**: Used to draw a bitmap as a texture. It can repeat or stretch the bitmap across the drawing area.
    * **RadialGradient**: Creates a gradient that transitions between colors along a straight line.
    * **SweepGradient**: Produces a gradient that sweeps around a central point in a circular fashion.
    * **ComposeShader**:  Combines two shaders using a blending mode.
    * **RuntimeShader**:  Introduced in Android 13, this allows developers to define custom per-pixel effects using the Android Graphics Shading Language (AGSL).

* **MaskFilter**: Base class for object that perform transformations on an alpha-channel mask before drawing it. A subclass of MaskFilter may be installed into a Paint. Blur and emboss are implemented as
  subclasses of MaskFilter. BlurMaskFilter only blurs the alpha mask of what you draw with a Paint in onDraw()—it creates a soft edge/“glow”. It does not Gaussian-blur arbitrary view content, and it doesn’t
  affect child views drawn by the framework. It also often requires software rendering (setLayerType(LAYER_TYPE_SOFTWARE, …)), so it’s the wrong tool for a container blur. It looks good on text, you can do
  `getPaint` on an TeztView

* Live wallpapers: They make use of the ``
```
class MyWallpaperService : WallpaperService() {
  override fun onCreateEngine(): Engine = WallpaperEngine()

  private inner class WallpaperEngine : WallpaperService.Engine() {

    override fun onTouchEvent(event: MotionEvent?) {
      if (event?.action == MotionEvent.ACTION_DOWN) {
        val canvas = surfaceHolder?.lockCanvas() ?: return

        val paint = Paint().apply {
          val randomColor = Random.nextInt(16_777_216)
            .toString(16)
            .padStart(6, '0')
          color = Color.parseColor("#$randomColor")
          style = Paint.Style.FILL
        }
        canvas.drawPaint(paint)

        surfaceHolder.unlockCanvasAndPost(canvas)
      }
    }
  }
}
```

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest>
  <application>

  <service
      android:name="MyWallpaperService"
      android:enabled="true"
      android:permission="android.permission.BIND_WALLPAPER">
    <intent-filter>
      <action android:name="android.service.wallpaper.WallpaperService" />
    </intent-filter>

    <meta-data
        android:name="android.service.wallpaper"
        android:resource="@xml/my_wallpaper" />
  </service>

  <uses-feature
      android:name="android.software.live_wallpaper"
      android:required="true" />
  </application>
</manifest>
```

* The `<merge />` tag is extremely useful and can do wonders in your code. However, it suffers from a couple of limitation:

  * `<merge />` can only be used as the root tag of an XML layout
  * When inflating a layout starting with `<merge />`, you must specify a parent ViewGroup and you must set attachToRoot to true (see the documentation of the inflate() method)
  * Assigning an id to it will do nothing

* `ViewStub` is a powerful variation of `<include />` that can help you further optimize your layouts without sacrificing features. You need to set the `layout` property to the layout you want to inflate into the `ViewStub`. When you are ready to inflate the stub, simply invoke the `inflate()` method. You can also simply change the visibility of the stub to VISIBLE or INVISIBLE and the stub will inflate. Note however that the `inflate()` method has the benefit of returning the root View of the inflate layout. It is very important to remember that after the stub is inflated, the stub is removed from the view hierarchy. As such, it is unnecessary to keep a long-lived reference, for instance in an class instance field, to a ViewStub. It's cheap and easy. The only drawback of `ViewStub` is that it currently does not support the `<merge />` tag.

* `Windwow`, `DecorView`, `ContentView`: When you setup your user interface by calling `setContentView()` on an Activity, Android adds your views to the Activity's window(its type is `PhoneWindow`), The window however does not contain only your views, but a few others created for you. First, the window creates `DecorView`. The `DecorView` is the view(FrameLayout) that actually holds the window's background drawable. Calling `getWindow().setBackgroundDrawable()` from your Activity changes the background of the window by changing the DecorView's background drawable, it also contains the system bars(status bars), the next node in the hierarchy is a linear layout(direct child of decor view) which I'm not sure how it is called, I think it is called "System Layout", this linear layout hosts the action bar/toolbar, but also a `FrameLayout` which is known as `ContentView` and the content view is the direct parent of our UI, so when you call `setCoentView` it is effectevely added to the decor view but its direct parent is content view, you can access the cotent view with `findViewById(android.R.id.content)` a the decor view with `getWindow().getDecorView()` . If you ever have an activity which background is not visible, probably because there is an opaque view on top of it like an `ImageView`, `MapView` or `WebView`, you can gain a little performance by setting the Activity's theme with the attribute `android:theme=@style/Theme.Custom` in the manifest inside the `<activty />` or `<application />` tag. According to [this document](https://android-developers.googleblog.com/2009/03/window-backgrounds-ui-speed.html), Android UI toolkit is not smart enough to prevent the drawing of views hidden by opaque children so we can leverage this optimization
    ```
    <resources>
      <style name="Theme.NoBackground" parent="android:Theme">
          <item name="android:windowBackground">@null</item>
      </style>
    </resources>
    ```
*  "focusable in touch mode" refers to a special mode that allows certain views, like EditText or AutoCompleteTextView, to receive focus when the user enters touch mode. This concept and "touch mode" seems to be very important previously in Android because in the past not all phone where touchscreen but now pretty much all devices are touch screen, touch mode is entered when the last interaction withn an app is from the touch of the screen

*  `OnClick` vs `OnTouch`: touch can acomplish the same functionallity as click but not the way around, this is because click is a very simple callback and doesn't provide much info about the event while touch
  provides a lot info about the event, you can distinguish gestures and position.

* **Event Handling System**: Events represent user interactions with the application, they are managed by the Android framework and handled using event listeners and event handlers

* `CollapsingToolbarLayout`: It wraps a `Toolbar` and at the same time it should be wrapped by an `AppBarLayout`

* `AppBarLayout`: There is a few interesting use cases like when wrapping a `CollapsingToolbarLayout` with this view, also when creating a app bar(also called toolbar) with more than one child like when wrapping a `Toolbar` and a `TabLayout` with this view and in this use case usually there is a `CoordinatorLayout` wrapping the `AppBarLayout` to enable animations. Basically this view lets you create interesting and very custom app bars/top bars/toolbars 

* `TopAppBar`/`Toolbar`/ `ActionBar`: Top app bar is a design concept, however in the compose context `TopAppBar` is the top bar composable which is equivalent to `Toolbar` and `ActionBar` in the view system. They are basically the same visual component but with the different implementation names you can find it with the concept name and also the names seen during its evolution throughout time, and basically they have different features basically the newer implementations have more "up to date" features and offers backward compatiblity, the newest implementation being `Toolbar`(Defined in the AndroidX AppCompat library) which can be found in material design. You might not want to set up your `toolbar` with Navigation controller so in simple use cases where you might not need to link it to ta navigation graph just set up your toolbar with the following function `setSupportActionBar(myToolbar)`, Be aware that the reason you should replace an `ActionBar` with a `ToolBar` as [described here](https://developer.android.com/develop/ui/views/components/appbar/setting-up) is that, since the apps come by default with an `ActionBar`, its functionallity depends on the platform it is running on, it will have different functionallity available so for this reason is best to use something more stable which is the main reason you want to hide the action bar(setting the application theme to `@style/Theme.AppCompat.Light.NoActionBar`) and replace it with the "AndroidX AppCompat library" `Toolbar` since you can control its version you will get backwards compatibility and stability

* `DrawerLayout` with `NavigationView`: UI component implemented in material design library, they are both used in conjuction and it usually even adds another views, a `DrawerLayout` wraps a `CoordinatorLayout`(this one wraps the app content) and below it we add a `NavigationView`, check implementation details in the [android material components repo](https://github.com/material-components/material-components-android/blob/master/docs/components/NavigationDrawer.md), btw in the docu you can see it is being deprecated in material 3 and you're adivced to use a [navigation rail](https://github.com/material-components/material-components-android/blob/master/docs/components/NavigationRail.md#adding-navigation-rail) instead

* **Dialogs**: If your app uses Activity 1.5.0 or higher, you can implement custom back navigation for a dialog by using `ComponentDialog` and its `OnBackPressedDispatcher`. `AlertDialog`s implement that interface so just by creating that type of dialogs you can get an instance of the back dispatcher.

* [Show a dialog fullscreen or as an embedded fragment](https://developer.android.com/develop/ui/views/components/dialogs#FullscreenDialog): First override `onCreateDialog` to add the below code
  ```
  Dialog dialog = super.onCreateDialog(savedInstanceState);
  dialog.requestWindowFeature(Window.FEATURE_NO_TITLE);
  retun dialog;
  ```
  Then in your activity or fragment call something like this
  ```
  if (isLargeLayout) {
      // The device is using a large layout, so show the fragment as a
      // dialog.
      myDialogFragment.show(fragmentManager, "dialog");
  } else {
      // The device is smaller, so show the fragment fullscreen.
      FragmentTransaction transaction = fragmentManager.beginTransaction();
      // For a polished look, specify a transition animation.
      transaction.setTransition(FragmentTransaction.TRANSIT_FRAGMENT_OPEN);
      // To make it fullscreen, use the 'content' root view as the container
      // for the fragment, which is always the root view for the activity.
      transaction
        .add(android.R.id.content, myDialogFragment)
        .addToBackStack(null)
        .commit();
  }
  ```
  To define the `isLargeLayout` variable use two resource qualifiers `res/values/bools.xml` and `res/values-large/bools.xml`
  ```
  <!-- true in large and false in regular size -->
  <resources>
    <bool name="large_layout">true</bool>
  </resources>
  ```
  Then consume it with
  ```
  isLargeLayout = getResources().getBoolean(R.bool.large_layout);
  ```

* [Show an activity as a dialog on large screens](https://developer.android.com/develop/ui/views/components/dialogs#ActivityAsDialog): To show an activity as a dialog only on large screens, apply the Theme.Holo.DialogWhenLarge theme to the <activity> manifest element:
  ```
  <activity android:theme="@android:style/Theme.Holo.DialogWhenLarge" >
  ```

* **Views that can be integrated with NavController**: `TopAppBar`/`Toolbar`/ `ActionBar`, `CollapsingToolbarLayout`, `AppBarLayout`, `DrawerLayout` with `NavigationView`, and `BottomNavigationView`

at the top of the screen with action items that affect the selected item(s). 

* **Action Mode/ActionMode**: Things I know I can do with this API is create a temporary top bar that contains clickable actions(icons), this top bar is displayed on top of the app's topbar if there is one already and if not it will create one and display, it contains action items that affect the selected item(s). When this mode is active, users can perform an action on multiple items at once. This CAB isn't necessarily associated with the app bar as they operate independently, although the contextual action bar visually overtakes the app bar position, its UI is usually presented either when a click/touch and hold(LongClick) happens or items with a chedcbox are selected, this mode is represented by the `ActionMode.TYPE_PRIMARY` flag, the actions/menus created on the bar that is on top of the app's bar is known as **contextual action mode** menus, and the bar containing them is commonly referred to as **Contextual Action Bar**(CAB), Check [this documentation](https://developer.android.com/develop/ui/views/components/menus#CABforViews) to see how to implement it, below we also explain how to do it. The other thing we can do with it, is enable text selection in `TextView`, `EditText`, and `WebView`. In a `TextView`, for example, when you select text you will see options displayed on top of it and you will see default options("copy" and "paste") but you can add your custom options by passing `true` to the `TextView` method `setTextIsSelectable`, then pass a callback to `setCustomSelectionActionModeCallback` in which you can add, remove options to the menu displayed by the `ActionMode`, in text views when implementing this logic you will need `TextView`'s methods like `getSelectionStart()` and `getSelectionEnd()` to perform your actions, these are called **context menus** or **floating context menu**, and they are enabled by the `ActionMode.TYPE_FLOATING` mode. You can display either the context bar menus or the floating context menu from activities or fragments, if you want to launch a context bar from a fragment you need to do it through the hosting activity, and if you want to display a floating context menu from a fragment you can do it directly with the functions in its class definition as well by overriding some methods.

* **Types of Menus**: We can find **"Options Menu"s**, in an activity override method `onCreateOptionsMenu`(always call `return super.onCreateOptionsMenu(menu);`) and handle clicks in the menu items overriding `onOptionsItemSelected` and in a Fragment you need to get instance of activity, parse it to `MenuHost` and add a provider with the method `addMenuProvider` if you need to navigate when an option is selected then in the "options selected" method return the boolean value with the following code `NavigationUI.onNavDestinationSelected(item, navController) || super.onOptionsItemSelected(item);`. If you want to modify/update your menu at runtime check the [official documentation](https://developer.android.com/develop/ui/views/components/menus#ChangingTheMenu), basically you just call `onPrepareOptionMenu()` to get an instance of the Menu object previously created, this method is available from activities and fragments. Other type is **"Contextual Menus"**, and inside it you have two different options, first is "Contextual App Bar Menus", these menus live inside a "contextual action bar"(CAB), they offer actions that affect a specific item or group of items, very commonly used with recyclerview items, the second type/options is "floating context menus" which are displayed similar to a dialog. You can create either or type of contextual menus from an activity or fragment, the bar context menu requires a few steps, first create the `<menu>` resource in `res/menu`, then implement the `ActionMode.Callback` somewhere in your activity(i.e an anonymous object), in that interface you override the necessary methods, in one of them you will inflate the menu with `actionMode.menuInflater.inflate(R.menu.menu_contextual, menu)`, the method will give you the necessary objects, then set the action that triggers the menu creation which usually is a long press/long click so do `view.setOnLongClickListener` in that callback return false if a reference to the bar context menu object already exists but if not you need to create the object/menu(keep a reference to it to be able to do the check) by calling `myReference = startActionMode(actionModeCallBackImpl)`, that method is available in activites and this will launch a top bar of type `ActionMode.TYPE_PRIMARY`, there is a variant of this method in which you can pass the callback instance and the action mode type you want to use, if we explictly ask for a floating menu `ActionMode.TYPE_FLOATING` but for this instead of using an `ActionMode.Callback` use `ActionMode.Callback2`, which provides a method in which you can set the position you want the menu to be displayed in, if you want to launch this from an activity remember you have to do it through an instance of the hosting activity to be able to execute the "start action mode" method, for floating contextual action menus we pass the view you want to be attached the menu to to the method `registerForContextMenu(view)` and override the method `onCreateContextMenu`(here you inflate the menu with menuInflater calling `getMenuInflater`) and `onContextItemSelected`, the three methods mentioned for this option are available in activites and fragments individually without needing to reference the hosting activity, there is some limitation with contextual menus, you have limited and none theming customization, the CAB is the only thing you can customize a little but floating action menus seems to be not theme wise customizable. Under the hood CAB uses the AndroidX's `<ActionMenuView>` which you can also use manually, aparently you could not use the CAB APIs at all and do it manually, however the floating context menus(context menus in short) don't offer a widget and they instantiate only a view called `ContextMenu`, so the first one uses a widget and that and we could use the same widget in our xmls but the second one is just a view but not a widget and is actually and interface so it can not be instantiated directly. The third type of menu(after contextual menus) is a **"Popup Menu"**, this isn't the same as a context menu, which is generally for actions that affect selected content. For actions that affect selected content, use the contextual action mode or floating context menu. You can create a popup menu with the below code, if you need a custom floating context menu following your theme or specifications then this would be a great option. BTW Icons at the top app bar are usally called **"Actions"** and options that don't fit inside the top app bar and are displayed in a three dot icon are called **"Overflow menus"**.

```
PopupMenu popup = new PopupMenu(this, v);
    popup.getMenuInflater().inflate(R.menu.actions, popup.getMenu());
    popup.show();
```

* [Action Views and Action Providers](https://developer.android.com/develop/ui/views/components/appbar/action-views): Now that we have talked about action mode and menus, you can see those concepts are related, and at the same time those concepts are related to this one and we will even expand those further when we talk about other APIs below, like performing a search with a `SearchView`, or `ShareActionProvider`(used to easily add a share button to your app’s ActionBar or Toolbar). Basically both, action views and action providers are visual components that live in the action bar/toolbar/top app bar, **action views** are for example, again, a `SearchView` and **action providers** are for example, also again, a `ShareActionProvider`, basically an action view lets you perform some action on the top app bar without having to change activities or fragments while action providers initially appear as a button(on the top app bar) or menu item(used in the options of menus like in overflow menus on the top bar when using option menus, CAB, floating contextual menus, and popup menus maybe). To implement an **action view** in a menu you do that in the items definition(Menu xml file), to an item element set one of the following attributes `app:actionViewClass`(class of a widget that implements the action) or `app:actionLayout`(layout resource describing the action's components), then set `app:showAsAction` attribute to `ifRoom|collapseActionView` or `never|collapseActionView`(`collapseActionView` flag indicates how to display the widget when the user isn't interacting with it), you can configure the action inside activity's `onCreateOptionsMenu` method, in there find the menu item in which you declared the previously mentioned properties with this code `menu.findItem(R.id.action_search)` with the `MenuItem` object obtained you can write the following line `SearchView searchView = (SearchView) searchItem.getActionView();` and here you have a reference to the action view, in this case a `SearchView`, that you can add listeners to configure any action you want to perform when user inputs some text. An action view can expand and you can add listeners for that in the same method we just mentioned(`onCreateOptionsMenu`), create a listener with `new OnActionExpandListener()`, and use the menu item we are configuring as an action view(we already showed how to find it) and pass it here `MenuItemCompat.setOnActionExpandListener(actionMenuItem, expandListener);`. To add an **action provider**, similarly to the action view, set the property `app:actionProviderClass` to the fully qualified class name for the action provider class, for example `app:actionProviderClass="androidx.appcompat.widget.ShareActionProvider"`, and also set `app:showAsAction="ifRoom"`, be aware it's unnecessary to declare an icon for the widget, since `ShareActionProvider` provides its own graphics. If you are using a custom action, declare an icon.

* `Backdrop` Vs `BottomDialogSheet`: These are UI components defined in Material Design 2, the former is used as a main container while the latter is used to provide optional/additional information. The former has concepts like front and back layers while those concepts don't exist in the later, they might look similar at first but they are actually different.

* [Settings Component](https://developer.android.com/develop/ui/views/components/settings): This API is used to design a native look settings screen fragment(implementing `PreferenceFragmentCompat`), we can find [native design guidelines](https://source.android.com/docs/core/settings/settings-guidelines) to create these screens and according to the official documentation, the settings are stored by default using `SharedPreferences`

* [Search Component](https://developer.android.com/develop/ui/views/search): You can implement this as a `SearchView` view which can be added in the top app bar as an action in the options menu of the top app bar if you want the search UI to be displayed when the user taps an icon of your choice on the top app bar that you want to set up as the search view "starter" then you have to configure it when creating the menu, if for any reason your icon is added to the overload menu of the top app bar then it won't display the search UI on the top app bar in which case you have to be prepared for this scenario and make sure to setup the other option to display a search interface, which is using a `Search Dialog`, if you can be sure the icon will be always visible in the top app bar then with the search view is enough otherwise you should be prepared with both. Be aware it doesn't have to be implemented in the top app bar, however it usually is. After you have the UI you have to configure how your search interface will behave, with an xml file of type `<searchable>`(this type of configuration can not be created at runtime), after you create a configuration you need to create an Activity, this is because when a search is performed, with these UI components, an intent is created and you the search activity you created is started and is passed the intent to it, the intent will contain some extras you can use to implement the search logic, then you can implement things like voice search(this uses the recognizer API under the hood), You can also provide search suggestions based on recent user queries(uses a content provider of type `SearchRecentSuggestionsProvider` under the hood, btw this type of result seem to only support text so you would have to work your logic using strings), or you could provide custom search suggestions that match actual results in your application data(it also uses a content provider under the hood but here you have to create a database table in which you can configure lots of options like an intent that will be launched when the search result is clicked, also include another column to store an extra that will be added to the intent that is launched when a result itme in clicked), the last feature we will talk about is to offer your application's search suggestions in the system-wide Quick Search Box(for this you have to implement the previous feature of custom search results).

* [AppSearch](https://developer.android.com/develop/ui/views/search/appsearch): This API is commonly used along with the search component. Lets explain it with an example, [this](https://github.com/android/search-samples/tree/main/AppSearchSample) note taking app uses `AppSearch` to index a user's notes and allows users to search over their notes, you can also see the `SearchView` used in combination with `AppSearch` in this example. The reason behind using this API could be to have a fast, mobile-first storage implementation with low I/O use, also it offers highly efficient indexing and querying over large data sets, and has relevance ranking and usage scoring. All these features of this API are great but maybe the one almost everybody looks at first is that is very performant, it offers lower latency for indexing and searching over large datasets **compared to SQLite**. AppSearch simplifies cross-type queries by supporting single queries whereas SQLite merges results from multiple tables.

* **Recognizer**: There are different types of recognizers that help do things like, Speech to text/Speech Recognition(using `RecognizerIntent`) `SpeechRecognizer`. Optical Character Recognition (OCR) in Android allows you to extract text from images or videos, you use ML Kit (Google's Machine Learning Library) to interact with the `TextRecognizer` API which is used to recognize text in images or video, such as street signs, it seems to be possible to use an implementation of the `TextRecognizer` API with the "Google Vision API". Fo the Speech recognizer, it should live in the viewmodel because it will return info that updates the state of the app, but it needs a context to get an instance of it, but wait, we can actually get that using [`CreatingExtras`](https://developer.android.com/topic/libraries/architecture/viewmodel/viewmodel-factories)
 
* **GestureDetector**: AI Edge Gesture Recognizer, A Google AI solution that uses machine learning to detect hands and recognize specific hand gestures in still images, video files, or live video streams.

* **Event system/Input Events**: Knowing how to handle events inside a custom view can help you create very custom user experiences and that to me is the most important reason why you should know how events work and what you can do with them. Be aware that now a days most events occur in "touch mode", as in the past devices very commonly had phyisical keyboards, trackballs, and that is how the touch mode was born, while we explain the differences betwen using the trackball and the touch screen we will see how the focus concept changed. Pretty much, touch screen replaced both keyboards and trackballs. When trackballs were common in devices, developers had to handle focus very meticulously because when using the trackball you had to navigate through the view hierarchy, for example you had a screen with two buttons, using the trackball you will select the second botton and then click it, usually the first button will have focus by default, but the focus had to be visual also(something like a highlight or some outline), but when devices started transitioning from trackball to touch screen they would usually have both, a trackball and a touch screen, so when the user was using the trackball and switch to using the touch screen, the device also switched to something called "touch mode", in here focus in most views is not visual(edit texts might be an exception to this behavior) if you want a view to be focusable on touch mode then set `focusableInTouchMode` to true, this means if you touch the view it receives focus, edit texts are focusable in touch mode by default. There is other property that actually can determine the focus on touch mode functionality, that is `focusable`, if you explicitly set it to false then the view won't be focusable in touch mode either, and if you set focusable in touch mode to true and don't set this property explictly then it is set to true automatically. So as you can see focusable is somthing like a general purpose property while focusable in touch mode is specific for working when in touch mode. Now a days, pretty much all devices are touchscreen, that means touch mode is pretty much always on, so now when an activity is rendered, acording to the "input events overview", focus is not assigned automatically, you hace to explicitly request that for a view, if there is two edit text view edit text, you can request focus programatically or just tap it and receive focus(edit texts should have visual focus, means "focusableInTouchMode" is true). Buttons are touchable but usually not focusable, when they are touched they don't take focus(if `focusable` or `focusableinTouchMode` is false), they simply fire the touch/click events/listeners. Another difference between `focusable` and `focusableInTouchMode` is that the former allows a view to be focusable through a keyboard or direactional pad(d-pad), that means, through hardware input events as opposed to `focusableInTouchMode` which grants focus only on software input events(touche events). So we can see that the "focus" concept is very important in how events work. Once the keyboards and trackballs dissapear, we were only left with touch screen devices, usually touch screen devices are said to only have software input methods(soft keyboard, voice input), and when having keyboards or trackballs, you will be able to track hardware input methods events, a very specific example is the view's interface `onKeyListener` and the activity's methods `onKeyDown` and `onKeyUp`, these three scenarios are only called with hardware key events, if you want to react to events in the IME(input method editor) AKA "soft keyboard", then you have to combine two approaches, for letters/characters/symbols/numbers use a `TextWatcher` object and pass it to an edit text `addTextChangedListener`, and for clicks on IME actions like "next", "done", set the edit text's `setOnEditorActionListener` method and compare againts things like `EditorInfo.IME_ACTION_DONE`, so software key events basically can only be tracked in input text views. There is two ways you can handle events, using either **event handlers** or **event listeners**, events behavior can be defined/customized by either overriding it's methods in a custom view, for example to handle a click which is actually a series of touch events, you can override `onTouchEvent()` or `onFocusChanged()` methods, or you could override an event listener setter method(`set`.. `OnClick`, `OnLongClick`, `OnFocusChange`, `onKey`, `onTouch`, `onCreateContextMenu`, ...`Listener`) these two options might be what you need when creating a custom view, however most commonly you will define event behavior by instantiating event listener interfaces and passing that instance to the right event listener method, for example instantiate an `OnClickListener` and pass that to `setOnClickListener`, which is very commonly done in the `onCreate` method of an activity, you could implement the interface in your activity or fragment, override the respective method and pass the activity or fragment instance to the listener's setter method, this apporach avoids the overhead of instantiating interfaces objects. There is three useful methods that are not part of the View class that hepls us manage events, `Activity.dispatchTouchEvent(MotionEvent)` - This allows your Activity to intercept all touch events before they are dispatched to the window, `ViewGroup.onInterceptTouchEvent(MotionEvent)` - This allows a ViewGroup to watch events as they are dispatched to child Views, `ViewParent.requestDisallowInterceptTouchEvent(boolean)` - Call this upon a parent View to indicate that it should not intercept touch events with `onInterceptTouchEvent(MotionEvent)`. If you ever need to define an order in which focus travels through the view hirearchy then use these methods `nextFocusDown`, `nextFocusLeft`, `nextFocusRight`, and `nextFocusUp` in the XML declaration. Knowing how to use input events we can manage our custom views scroll behaviour and customize things like panning, fling and drag(scrolling), using events we also can enable zooming in a custom view. An important API in detecting scroll, fling, and other gestures is the `Scroller` and `OverScroller`

* **Touch Gestures**: These concepts are very closely related to the event system/input events, check [material design gestrues](https://m2.material.io/design/interaction/gestures.html#types-of-gestures) and [material motion](https://m2.material.io/design/motion/understanding-motion.html) to see some guidelines on how gestures should work but I will list important and key words/concepts mentioned in these guides but without going in detail what they are about, they are: types of gestures are "Navigational gestures"(tap, scroll and/or pan, drag, swipe, pinch), "Action gestures"(tap in an FAB displays a menu, long press to enter a "selection mode", swipe to dismiss/delete/bookmark), "Transform gestures"(double tap or pinch to zoom in and out, compound gestures like using pinch to zoom in/out, rotate, and/or panning, another transform gesture is somewhat also a compound gesture because you could combine long press with dragging to reorder a list). Motion(you could understand motion as some sort of animations, an example can be selecting a message from inbox and seeing it displaying the message in full screen but with some animations and transformations, also something more simple it could be swithching pages using a view pager along with tab layout views, and any type of animations that helps create visual focus like waves around a phone icon when making a phone call or express emotions like celebration using an animation/motion when clicking something, etc. TODO In [this](https://m2.material.io/design/motion/understanding-motion.html#brand-expression) document we can see very interesting animations and transitions, It would be a great challenge to acomplsh one or more or these animations, FYI I already tried searching crane's repo but that app got actually "deprecated", I tried downloading the source code since I found it in an android's material repo but that version didn't have an animated Icon, basil's app code doesn't even exits publicly so basically you're on your own on this challenge) helps users understand the UI, it makes it informative by highlighting realiationships between UI elements, it expresses hierarchy by showing how elements in a visual transition, using motion, are related,

* [**What APIs can we use to create animations**](https://youtu.be/N_x7SV3I3P0?si=GIcTJfOBncTbWEfD): There is a variety of APIs we can use, we have the view animations which is consider a legacy API they live in the `res/anim` directory and it contains legacy view animations, then we have the animator property APIs, which include `ValueAnimator` and `ObjectAnimator` and those files live in `res/animaror`, these files are referenced from the UI layer with `R.anim.id` and `R.animator.id` respectively. The legacy view animations are executed in the draw phase an is still used in window animations and fragment transactions(here you can use either or actually), the fact that view animations run in the draw phase of the view system might be a reason why to still use them since this guarantees the animation will run after the view has been measured and laid out. There is other APIs built on top of the animator APIs, these are the following, `ObjectAnimator` when using it on views avoid passing the string such as "alpha", eventhought this works, it is more costly as it uses reflection to find the setter and getter of that property, instead enter `View.ALPHA`, you can combine run animations in parallel using `PropertiesValuesHolder`, create it with `PropertyValuesHolder.ofFloat(View.SCALE_X, 0.5, 1f)`, create as many as you need and then pass it to a `ObjectAnimator.ofPropertyValuesHolder(view, propValueHolder1, propValueHolder2)`, and use interporlators like `OvershootInterpolator()`, you can actyally define an `<objectAnimator>` with `<propertyValuesHolder>` tags inside it in XML, so basically you execute animations in parallel/asynchronosuly with this API, but if you actually want to execute animation synchronously(one by one), use `AnimatorSet`, it can also execute animations in parallel though, basically it allows you to coordinate the order in which animations are executed, we also have a `ViewPropertyAnimator`, this one is not an animator as such but is backed by a `ValueAnimator` and it is very convenient actually, you just call `view.animate().scaleX(1f).alpha(1f).setInterpolator(OvershootInterpolator()).start()` on any view and you get an animation, very convenient, it doesn't offer a way to know the progress, reverse the animation or run it in cycles directly using its API, it is sligthly more efficient that `ObjectAnimator` since it has been optimized to animate views, is a fire and forget approach, `ValueAnimator` almost always is used by defining an update listener since it doesn't act on a view implicitly, you have to be explicit and declare code that changes a view property inside the listener, you can use anything inside here like updating a spannable string, `AnimatedVectorDrawable` under the hood it is basically wrapping a vector drawable and an animator(one or more Object animators), they implement the animator interface so we can start animations, AVD runs in the render thread which makes animation run smoothly and separately from the main ui thread, its API is very simple just start and stop, no interpolators, seek, restart, etc, if you need to control progress/seek use lottie or kyrie drawables, we also have "Physic-base animations" which are used to carry on the momentum of a gesture and create a sense of continuity, they are interrupible meaning a user can do other gestures while the physic-base animation is running and it will process the changes in its course, so to do this first you would add an on touch listener just call `setOnTouchListener` on a view and pass an instance of the interface, the first thing you want to do is call `event.setLocation(event.rawX, event.rawY)`, this event object is the argument we received from the overriden method, then call `tracker.addMovement(event)` this tracker is of type `VelocityTracker`(a class defined in the `View` class), now you want us a when(Kotlin) statement to analyze the event action(`event.actionMasked`), capture the x and y of the first down action using a `Point`(firstDown), but also at the same time record in another `Point`(loc) your view's x and y using its layout params. in the actions move event(`MovementEvent.ACTION_MOVE`), calculate the movement x and y length by substracting `event.x - firstDown.x`(`resultX`), and then add that result to the loc point and assign it to the layout params(`layoutParams.x = lox.x + resultX`), this way you view will be moved across the screen, inside the same event after doing these operations do execute `windowManager.updateViewLayout(myView, layoutParams)` this code will actually make your view move across the screen by updating the views layout, finally the action up is captured, call `tracker.computeCurrentVelocity(1000)`, then store `tracker.xVelocity`(val velocityX) in a variable, and use it to assign a value dinamically to another variable with the following evaluation, `val finalPos = velocityX > 0 ? screenSize.x - v.width : 0f`, and now finally you use the physics base animations `val animX = SpringAnimation(view, ParamXProp(), finalPos).setStartVelocity(velocityX)` and `animX.start()`, the `ParamXProp` is a `FloatProperty`, actually a `FloatPropertyCompat`, this class is part of the `Property` classes but it avoids having to wrap primitive value inside an object and allows you tu use primitives directly which supposes a performance improvement, these classes are used in the classes like `ObjectAnimator`, in its primary constructor we set `name: String = "ParamX"`, and we should override its getter an setter, in the setter get the view's layout params, the view refernece is passed to you from a function parameter, and cast it to `WindowManager.LayoutParams`(this is done because this example is running the animation in an overlay) then set those param's x value to the animated value recieved in the mehtod's arguments `params.x = animValue`, and then call `this@MainActivity.windowManager.updateViewLayout(view, params)`, in the getter, just execute `(view.layoutParams as WindowManager.LayoutParams).x.toFloat()`, this is done because the view that we are animating was created in an application overlay window. `Transitions`, you can think of it like a factory for creating animators based on two layouts which you will transition to and from each other(this animates the view state changes automatically), for that we usually use `TransitionManager` but in here we will see how to use `MotionLayout` later, but first lets talk about how to use transtition manager, first you call it with `TransitionManager.beginDelayedTransition(viewGroup)`, then make some changes to the view hierarchy and then "magic" happens, what that magic actually does is, it looks between two different states of the UI and works out what's changed and creates an animator, you could also do some transitions with pure animations(without `TransitionManager` nor `MotionLayout`) but it might consume more time doing the "manually", there is a lot of times when you want to run transitions(using animations only) on a scrim/overlay, and for that we need listeners/callbacks to execute animations only when a screen or view already has all its info, meaning that it has been measured and laid out but not drawn on screen yet, so we can use callbacks like `view.viewTreeObserver.addOnPreDrawListener()`, previously I found other ways that we can observe and excecute code when a view has been meassured and laid out in [this](https://cheesecakelabs.com/blog/understanding-android-views-dimensions-set/) article we see solutions like `view.Post`, `View.OnGlobalLayoutListener`, `View.OnLayoutChangeListener`, these three could work but they all have caveats if we are using them to run animations, we should only run animations after the view has be laid out and meassured, in `Android Ktx` library  we can find a "better" option than those three since we want to get a callback only when the view has been laid out and meassured, this means we can access the view's width and not measured width only, as well as its position on screen, that is `view.doOnlayout`, it even makes sure to run it if it is attached after the view has already been laid out and meassured and we attached the listener after it, it usues `View.OnLayoutChangeListener` under the hood, but there is an even better solution we can use, `view.viewTreeObserver.addOnPreDrawListener()` remember you almost always want remove this callback after the first time this call back is executed, however you almost always now use the transitions API to do transitions, meaning either using `TransitionManager` or `MotionLayout` instead of pure animations running inside on pre draw listeners, if your using `TransitionManager` you can create your custom transitions(animations) by extending the already existing ones like `Visibility`, which others like `Fade`, `Slide` extends from, or you can extend their common parent/super, `Transition`, and you can inflate them from a resource file if you defined it in `/res/transition` directory or you can instantiate it programatically(i.e. `new Fade()`) and pass that object to the `TransitionManager`, there are very cool things you can do when doing transitions, in transitions there is two views that can be thought as a common view in two different states of the app, the different states usually means different fragments or activities but be aware that they could be in the same screen just that the hierarchy state changed, so transitions are very commonly used to animate shared UI elements between fragments or activities, so between activities you can run transitions between shared elements but also you can run "window content transitions" which is usually run when the activity launches and exists this lets you run transitions on the activity content. You also can use `CoordinatorLayout` which is a superpowered FrameLayout, it has some predefined animations that implement Material Design elements such as `CollapsingToolbar`, FAB buttons that slides when a snackbar is being displayed, swipable content, you pretty much can accomplish these elements with any of the other options disscussed here, and you also can implement custom movement and animation rules via `CoordinatorLayout.Behavior`. We also have the option to animate a set of views that are children of a constraint layout with constraint helpers, btw you should know that you can create your own virtual layouts too(virtual layouts inherit from constraint helpers), conceptually all helpers are virtual views, using helpers to create animations in constraint layouts enables you to encapsulate the animation behavior, also makes you capable of applying that animation to a set of views at once, to do this you would use any of the animations APIs we already talked about, view animatiions, or you can use the new property animation(`ValuAnimator`, `ObjectAnimator`, `LayoutTransition`, this last one is also part of the proprety animation system, these layout transitions use animators under the hood so it is part of that system), to implement such a helper you would override `ConstraintHelper` method `updatePostLayout`, btw to make sure to run it only once you may want to keep a reference to the instance of the container in the parameters however this might not be suficient in configurations changes(not sure now), to do that first you first define the min and max radius, usually you start from 0, and you get a delta from two X coordinates and another delta from two Y coordinates and use them to calculate an hyptoenuse with a function like `Math.hypot(delta, delta)`, and find the center of the virtual view first finding its own center and then substracting that with `getLeft()` on the X axis, and `getTop()` on the Y axis, then you use the property animation utils class and call `ViewAnimationUtils.createCircularReveal(this, centerX, centerY, 0, radius)`, then just consume your helper as usual(just like you would create a `Barrier` in a constraint layout). You also have the option to use `MotionLayout`, and this is a more robust solution and therefore you should look for the section that explains its API more in depth in below notes. In short we have "View Animation", "Animator"(Value Animator, Object Animator, Animator Set), Built on top of animator we have "Animated Vector Drawables", "Layout Transition"(not an animator itself but is backed by an animator), "Animated State List Drawable", "View Property Animator"(this one is not an animator as such but is backed by "value animator"), "Property Values holder"(this one is used in conjuction with "Value Animator" and "Object Animator"),"Item Animator"(animations in recycler view, these can be used to animate adding and removing items as well as just animate items as they being scrolled like a scale animation), "Transitions" with transition manager most modern use case is to animate shared elements wehn navigating across screens(Acitivities and fragments) but you can also use it inside the same view, "Layout transitions"(not same as transition manager) this one uses animators under the hood also, you could coordinate some animatinos using "Coordinator Layout" and implementing custom behaviors,  besides these we have other APIs such as "Physics", and "Motion Layout" which we go more in depth in below notes, but FYI, it is a subclass of constraint layout.

* [How property animation differs from view animation](https://developer.android.com/develop/ui/views/animations/prop-animation#property-vs-view): In the view animation system if you animated a button to move across the screen, the button draws correctly, but the actual location where you can click the button does not change, so you have to implement your own logic to handle this. With the property animation system, these constraints are completely removed, and you can animate any property of any object (Views and non-Views) and the object itself is actually modified. The view animation system, however, takes less time to setup and requires less code to write. In order to use the property animation system you need to choose one of the `Animator` implementations, `ValueAnmator`, `ObjectAnimator`, and `AnimatorSet`, then you have to chose an evaluator implementation, `IntEvaluator`, `FloatEvaluator`, `ArgbEvaluator`, `TypeEvaluator`(Interface that allows you to create custom evaluators), so you can create an animator with the following code `ValueAnimator.ofFloat(0f, 100f)`, `ObjectAnimator.ofFloat(textView, "translationX", 100f)`, once you have an instance of an animator, you can set its time interpolator, duration, you can also set its `AnimatorListener` to listen star, end, cancel, and repeat, events, you could use `AnimatorListenerAdapter` which implements `AnimatorListener` with empty methods, its is just a convenience implementation, another listeners is `AnimatorUpdateListener` to listen for value updates.

* Animating `ViewGroup`s: To acomplish this task you have different options. First option is known as `LayoutTransition` animations, using property animation system, call `setLayoutTransition` on the viewgroup and pass a `new LayoutTransition()` object which you should configure assigning it an animator with its `setAnimator` method, which takes an event represented by an integer, you have four events to choose from `APPEARING`, `CHANGE_APPEARING`, `DISAPPEARING`, and `CHANGE_DISAPPEARING`, the "change" events are events that run on other views when another view is dissapearing or appearing, you'd probably create an animator for a group view like this `ObectAnimator.ofPropertyValuesHodler(null, PropertyValuHolder.ofFloat("scaleX", 1, 0), PropertyValuHolder.ofFloat("scaleY", 1, 0))`, if you are only animating appearing/dissappearing you could/should also enable animations for the other views that are being affected by inserting/removing a view with `transition.enableTransitionType(LayoutTransition.CHANGING)`, I'm not sure this option is usable with a `RecyclerView` but any other `ViewGoup` is supported. Another option is to animate view groups automatically using the attribute `android:animateLayoutChanges="true"`, I believe this option is not compatible with RecyclerView. Another option is `TransitionManager.beginDelayedTransition(viewGoupInstance, Fade())`, call this method and pass the view group instance before making any changes to children like changing visibility, modify width/height, change view possitions, add/remove views, you can pass it a `Fade()`, `AutoTransition()`, or `ChangeBounds()`, I'm not sure this works good with a `RecyclerView`. The last option I know is using the "old" view animation system, for that you use an API called `LayoutAnimationController` for that you have to define an animation resource of type `<layoutAnimation>`, this resources points to other animation resource files(both defined in `res/anim`) that at the same time define animations like `<alpha>`, `<scale>`, `<translate>`, or a `<set>`(this contains a group of animations), these xml resources tags are described in [this](https://developer.android.com/guide/topics/resources/animation-resource#View) documentation, with that resource in place you have to either pass it to the `ViewGroup`'s defniition in the XML or programatically, if you want to pass it via XML create two files inside the `res/anim/` folder and the first one is, as mentioned, an animation like `<scale>`, the other is a `layoutAnimation` which references the animation, layout animation can define attributes like `android:animation="@anim/item_animation_fall_down"`, `android:delay="15%"`, `android:animationOrder="normal"`, and then assign it to the `ViewGroup`'s tag using the attribute `android:layoutAnimation="@anim/layout_animation_fall_down"`, for passing it programatically, you actually could pass any of the two resources preivously mentioned programatically, `LayoutAnimationController controller = AnimationUtils.loadLayoutAnimation(context, R.anim.fade_in)` if you're loading the `<layoutaAnimation>` resource just pass it directly to `viewGroupInstance.setLayoutAnimation(controller)` and if you're loading the animation resource you have to create the animation controller first, `Animation animation = AnimationUtils.loadAnimation(context, R.anim.slide_in_bottom)` ,`LayoutAnimationController controller = new LayoutAnimationController(animation, 0.25f)`, configure with `controller.setOrder(LayoutAnimationController.ORDER_NORMAL)` and other methods and then pass it as the previous option using `view.setLayoutAnimation` method, this last API used is compatible with recycler view and we can find an example on how to use it to create a staggering/fall down/cascade animation [here](https://proandroiddev.com/enter-animation-using-recyclerview-and-layoutanimation-part-1-list-75a874a5d213) basically when the view is created either in `onViewCreated`(fragment) or `onCreate`(activity) assign the layout animation with `setLayoutAnimation` to the recycler view and then call `recyclerView.adapter?.notifyDataSetChanged()` and `recyclerView.scheduleLayoutAnimation()`, if you want to listen for events set an `recyclerView.setLayoutAnimationListener`, While it works for `RecyclerView`, if a user swipes too fast, the animations might be cut short because views are being recycled, other limitation is it is primarily designed to animate views appearing (entering) a layout, not for animating them out (leaving). So basically this last option uses an `Animation` object which is wrapped inside a `LayouAnimationController` which is then passed to the `ViewGroup`, there is an interesting detail here, under the hood you can define an `Animation` object either programatically or in XML, if you define it via XML you can use either system, the property animation system or the "legacy" view animation system, but if you want to do it programatically, an `Animation` instance can only be implemented using the legacy system, and you have to extend the `Animation` class for a custom animation or use one of its children like `TranslateAnimation`, this is because if you want to create an animation using the new property system you use a totally different class/method, extend different classes for custom animations, and uses a totally different API in general. BTW there is some helpers you should know `ViewAnimationUtils` whichs seems to be helpers that work with the new animtor APIs and the `AnimationUtils` which are utilities that help you do the legacy view animations. You also can use `CoordinatorLayout` which is a superpowered FrameLayout, it has some predefined animations that implement Material Design elements such as `CollapsingToolbar`, FAB buttons that slides when a snackbar is being displayed, swipable content, you pretty much can accomplish these elements with any of the other options disscussed here, and you also can implement custom movement and animation rules via `CoordinatorLayout.Behavior`

* [**RecyclerView Animations**](https://youtu.be/imsr8NrIAMs?si=QtYVPW1pc5qroopb): First you should know that you should never notify a huge number of data has changed with the code `adapter.notifyItemRangeChanged(0, itemCount)` because the recycler's view cache pool will not be able to recycle the existing views so if you really need to do that you should then call `recyclerView.getRecycledViewPool().setMaxRecycledViews(VIEW_TYPE_INT, max)` this should help to make the recycler reuse as much views as possible, but ideally you just call `adapter.notifyItemChanged(index)`, RV already offers animations out of the box, fade out animation runs for removed items, fade in is played for added items, translate for moved(rearranged) items, cross-fade for content changed items, you have to be aware of the possible item changes thought, they can persist, be added, be removed, be changed, appear, disppear, the last two are also called as predictive changes and they occur because an item was persited on a data set change but also they were reorder so you should be able to say where those items come from and where they are going. Layout manager is responsible for figuring out where items come from and where they go to, `ItemAnimator` runs the animations, so they work together, and you can customize your animations by [implementing your own layout manager](https://wiresareobsolete.com/2014/09/building-a-recyclerview-layoutmanager-part-1/) but you will have to enable predictive animations, that means you have to inform the manager where all persisted items where before it changed and where things need to be because of the change, you have to opt-in into predictive animations you will have two calls in the `onChildren` method and the first will be a pass that lets you record the original state and then another pass to figure out where things are when the changes applied, after you know what changed you also need an animator, and you can use the default one but mostlikely if you're reading this is because you want to customize things, and that is done by implementing a custom `ItemAnimator`, override `record[Pre|Post]LayoutInformation()`, `animate[Appeareance|Disappearance]()`, `animatePersistence()`, `animateChange()`, important thins is that when you're done with an animation in a viewholder, you should call `itemAnimator.dispatchAnimationFinished(viewHolder)` and `itemAnimator.canReuseUpdatedViewHolder(viewHolder)` these last two helps to get rid of some state that this component is doing and also helps recycle animated views meaning instead of creating new viewholders they can be returned from the cached after they are animated, when you implement your own item animator the method `canReuseUpdatedViewHolder` mostlikely should always return true so just return true always, you also may want to extend from `DefaultItemAnimator`, so thing is that you need to determined what changed and for that your calls from through the adapter to indicate an item changed should also send a payload, which is a list of objects, `adapter.notifyItemChanged(position, payload)`, this payload will travel down to the animator to methods that you need to override such as `recordPreLayoutInformation` and `recordPostLayoutInformation`, however the payload is not essential it would be more essential to create an implementation of `ItemAnimator.ItemHolderInfo` class which is the type of object that these two methods return, and since these two methods pass a reference to our view holder in the parameters we can actually extract the properties from it and store it in the custom item holder info class, and now you should override the animation behavior in `ItemAnimator.animateAppearance`, `ItemAnimator.animateChange`, `ItemAnimator.animatePersistence`, `ItemAnimator.animateDisappearance`, in each of these methods you should execute the function mentioned previously , `dispatchAnimationFinished(viewHolder)`, here we use any animation system we want. There is two positions in a recycler view, first, "adapter position" is the position of the object you are referencing in the list of objects that the recycler view is using to inflate/bind the viewholder, you can get this either from a reference of the recyclerview(`recyclerView.getChildAdapterPosition(view)`, the view argument is the view the adapter produced that lives inside the viewholder, this method calls the viewholder under the hood), from the adapter you also need to get a reference to the viewholder(`viewHolder.getBindingAdapterPosition()` or `getAbsoluteAdapterPosition()`, the latter is usually used if you created a `ConcatAdapter` by merging two or more adapters into a single adapter) but good thing is that the adapter happens to instantiate and configure the viewholders so it has pretty much direct access to the viewholder instancnes before they are cached, and from the layoutManager(`layoutManager.getPosition(view)`), this position, again, represents the index of the element in the data source list that is hosted by this view, the other position is layout position, and you can access to that info using the same three options, this position represents the index of the view on the screen, remember the point of recycler view is to render all items of the data source set using the least ammount of views(viewholders) this means those views are reused(recycle), viewholders are like empty shells that knows how to correctly display the source data list items, in an ideal scenario your recycler view and its components only creates and caches the necessary views to display the quantity of items that fits into the screen and reuses views from the cache when scrolling through the list, so this means that if the screen only fits 5 fully displayed viewholders then mostlikely it will only handle up to 5 plus a few more viewholders in its cache, imagine your source data list has 2000 items, you will only have around 7 views being reused to display 2000 items, if you scrolled to position 1000 of the source data set then the adapter position will match that index, and the layout position could be any index from 0 to 6(probably, in this scenario, you could only have 6 items in the screen with two not being fully displayed), so from recycler view `recyclerView.getChildLayoutPosition(view)`(this uses viewHolder under the hood), from an adapter you would use `viewHolder.getLayoutPosition()`, from layout manager `layoutManager.getPosition(view)`, btw views created by the adapter(these are wrapped arround a viewholder) hold a reference to the viewholder in their layout params, its is becuase the layout params they use is a custom implementation declared in recyclerview, this is kind of a cicular dependency because viewholder wraps the actual view(viewholder is a wrapper class, it is not a `View`'s child class) and that wrapped view has reference to its wrapper(viewholder) in its layout params you can use which means you can access the positions info from here also.
 
* Animating a Single View: Here you can use different methods, first option, using the old view animation API, you can load an `Animation` instance, it seems that under the hood an animation(the resource declaring the animation in the XML which by convention is declared in `res/anim/`) can be defined using either the "old" view animation system or the "new" property animation system as stated [here](https://developer.android.com/guide/topics/resources/animation-resource#View), you load it with the code `AnimationUtils.loadAnimation(context, R.anim.some_anim_xml_file)`, then call the `startAnimation()` method on the view you want to animate and pass the `Animation` object you just loaded/created to it, you can create this `Animation`s instances programatically and if you do so then you either extend that class for a custom animation or use the built-in animation implementations like `TranslateAnimation` and these objects are using the "old" view animation system, that means that the animation systems are separated programatically but they can be declared in the same XML file(only once at a time, is either or, not both) making it possible to switch between systems more easily, which leads us to the next option, the property animation system, if we want to animate view properties programatically just read the previous section("What APIs can we use to create animations") There we can see `ValueAnimator`, `ObjectAnimator`, `ViewPropertyAnimator`, `PropertiesValuesHolder`, and `TransitionManager`(it should be possible to animate single views with it too). You also can use `CoordinatorLayou` which is a superpowered FrameLayout, it has some predefined animations that implement Material Design elements such as `CollapsingToolbar`, FAB buttons that slides when a snackbar is being displayed, swipable content, you pretty much can accomplish these elements with any of the other options disscussed here, and you also can implement custom movement and animation rules via `CoordinatorLayout.Behavior`

* Animating State: Here we have two options/APIs, first, `StateListAnimator` are animations that are automatically triggered when the view state changes, it contains a list of states that we want to animate in defined in `<item>` tags which at the same time contains `<set>`(this one contains `<objectAnimator>` tags inside) tags, `StateListDrawable`, `ColorStateList`, all state list are defined in a `<selector>` and they contain `<item>` tags, some of the `<item>` tags contain other resources like `<shape>`(This actually creates a gradient drawable which is a type of drawable that lets to create shapes that has some color) but usually just define properties like `android:drawable=`(this one is any drawable) or `android:color=`(for color state lists), also `AnimatedStateListDrawable` this seems similar to Animated Vector Drawables but seem hard to use maybe AVD are better, they are defined in `<animated-selector>` tags and they contain `<item>` and `<transition>` tags, the former one defines states by using ids and the drawables they will display and the latter defines a "from" and a "to" states that references the previously defined ids and also the drawables that are displayed in between those states
`
* [`ViewPropertyAnimator` vs `ObjectAnimator`](https://android-developers.googleblog.com/2011/05/introducing-viewpropertyanimator.html): Here we can see that the first is a more performant way to do animations which is done by calling `animate()` on a view and then setting the properties we want to animate as if we were using a builder pattern

* **Custom Views**: When creating a custom view, if you want to use 3D graphics, extend `SurfaceView` instead of `View`/`ViewGroup` and draw from a separate thread. See the `GLSurfaceViewActivity`( implementation of `SurfaceView` that uses a dedicated surface for displaying a OpenGL ren>dering surface), otherwise if you want to do 2D graphics, or just a styled view, then you may need to override the `onDraw()` method which receives an argument of type `Canvas`. Another very important method is `onMeasure()` which plays a critical role in the contract between the custom view and its container, in its parameters it receives an integer value that represent size limits that its parent needs its children to take into account when definning its size and is very important to know that when you override this method, is very important to call `setMeasuredDimension()` which communicates our view size to its parent and system, otherwise an exception will be thrown, if you override this method you will receive, as mentioned, an object which is actually an `int` that is encoded and you need to decode using the static methods, `View.MeasureSpec.getSize(int)` to get the size in pixels of the area your view and its children will be drawn into, and `View.MeasureSpec.getMode(int)` which return either `UNSPECIFIED`, `AT_MOST` or `EXACTLY` so you know how your children and view size should behave. A very handy method you might like to use inside `onMeasure` is `resolveSizeAndState` which will help you reconcile the view's desired width/height with the constraint the parent imposed on it. If you want to have a view resized you can trigger the `onMeasure` method by calling `measure(int, int)` method in which the integers are encoded using `View.MeasureSpec`. [Here](https://developer.android.com/develop/ui/views/layout/custom-views/custom-components) is a short list of methods that the UI framework calls on views, you can see methods like `onMeasure`, `onLayout`, `onSizeChanged`(these 3 are related to the layout process), etc. [Here](https://android.googlesource.com/platform/development/+/refs/heads/main/samples/NotePad/src/com/example/android/notepad/NoteEditor.java) you can find custom view creating a "notepad", an interesting and important behavior is in the `onDraw()` method, note that if you want to draw something into your custom view's canvas you first have to draw whatever you want inside the overriden function and just before the method ends you have to call the superclass method, that is, `super.onDraw()`. You can define custom attributes for your custom view as explained [here](https://developer.android.com/develop/ui/views/layout/custom-views/create-view#customattr). To define custom attributes, add `<declare-styleable>` resources to your project. It's customary to put these resources into a `res/values/attrs.xml` file. The name of the styleable entity is, by convention, the same name as the name of the class that defines the custom view. Once you define custom attributes, you can use them in layout XML files just like built-in attributes. The only difference is that your custom attributes belong to a different namespace. Instead of belonging to the `http://schemas.android.com/apk/res/android` namespace, they belong to `http://schemas.android.com/apk/res/[your package name]`, in the XML, for example, you call it like this  `xmlns:custom="http://schemas.android.com/apk/res-auto"` and then inside your custom view element call one of the custom attributes like this `custom:attributeExample="true"`, you can change the `custom` alias to anything you want. If your view class is an inner class, further qualify it with the name of the view's outer class. For instance, the `PieChart` class has an inner class called `PieView`. To use the custom attributes from this class, you use the tag `com.example.customviews.charting.PieChart$PieView`. When you want to read this attributes from your view, you should knwo that a view that is created from an XML layout reads its attributes in the XML tag from the resource bundle which are then passed into the view's constructor as an `AttributeSet`, then pass that object to  `context.getTheme().obtainStyledAttributes()` method which returns a `TypedArray` array of values that are already dereferenced and styled and you obtain attributes with methods like ` typeArray.getBoolean(R.styleable.PieChart_showText, false);` you usually read its values insidie a try-finally block, in the finally block call `typeArray.recycle()` to recycle this object after use. According to Android's official documentation you should expose your view's custom properties using a getter and a setter, in the setter method you usually call `invalidate();` in case your view needs to be redraw and `requestLayout();` if a value change also changes its size, you usually also expose event listeners like to communicate when something changed in your view for example when changing color, size, etc. You should also design your view to be accessible for users with disabilites as explained [here](https://developer.android.com/develop/ui/views/layout/custom-views/create-view#accessibility). When you need to implement custom drawing use the `onDraw()` method, avoid instantiating objects as much as possible, also avoid making calculations in this method as views are redrawn frequently, it should only be used to tell the framework what you want to draw, for example if your view size changes don't calculate any necessary logic in here, for example if you're drawing a circle you should instead calculate the radius or diameter in either the `onSizeChanged` or `onMeasure` method, use the former if you don't need control over the views size but you need to calculate things like radius, diameter, etc, and the latter when you need finer control over the views size, you should be able to perform any work you do in the former one in the latter one but try to keep it simple, that means, if you don't need to override the later just use the former(check [here](https://developer.android.com/develop/ui/views/layout/custom-views/custom-drawing) for official documentation on creating custom drawings. 

* **How cusstom views are drawn**: When an activity receives focus it passes its views hierarchy root node to the Android framework which then draws the node tree by traversing the tree in a pre-order manner, meaning the parent is drawn before its children and then sibblings are drawn in the order they appear, each `ViewGroup` is responsible for requesting each child to be drawn by using the `draw()` method, and each child is responsible for drawing itself. The framework doesn't draw View objects that aren't in a valid region. You can force a View to draw by calling `invalidate()`.

* Views/Widgets: Some interesting widgets, to name a few, are `Spinner`, `ViewPager`(with `TabLayout`) and `ViewPager2`, `AutoCompleteTextView`, `ImageSwitcher`, `TextSwitcher`, and `ViewSwitcher`,  `SlidingPaneLayout`(is a two-pane layout use in adaptive UI when displayed on large screens)

* `RemoteView`s: It seems like it's an API/concept that's been around for a while now, it is a way to define UI that other processes will display on your behalf, this is why it is used when creating App Widgets (Home Screen Widgets) which is the most common use case, Custom Notifications this lets you define a custom layout for your notifications, and when creating UI layouts for Android Automotive OS. When the RemoteView is sent to another process all the views that are part of its layout are serialized, this is why there is limited support for the built-in views that you can use and the level of customization you can get in this views. They support only limited click interactions via `PendingIntent`. Animations and complex gestures aren’t allowed. Modern alternatives are: Jetpack Glance (Recommended for Widgets), if you’re building rich notifications, use Android’s built-in notification styles (`MediaStyle`, `MessagingStyle`, etc.) before falling back to custom RemoteViews. Views are drawn using a two-pass process: a measure pass and layout pass. In the first pass parents pushes dimension specifications to their children and at the end of this process each view stores its measurements, in the second pass parents are responsible for positioning its children using the measurments from first pass. You can trigger a first pass with method `measure()`, when this method returns, you can get a view's measured size with `getMeasuredWidth()` and `getMeasuredHeight()`. A parent View might call measure() more than once on its children. For example, the parent might measure the children once with unspecified dimensions to determine their preferred sizes. If the sum of the children's unconstrained sizes is too big or too small, the parent might call measure() again with values that constrain the children's sizes. The measure pass uses two classes to communicate dimensions. The `ViewGroup.LayoutParams` which View objects use to communicate their preferred sizes and positions, and `MeasureSpec` objects are used to push requirements down the tree from parent to child(A parent usually uses `UNSPECIFIED` mode to determine the target dimension of a child View. For example, call `measure()` on its child with the height set to `UNSPECIFIED` and a width of `EXACTLY` 240 to find out how tall the child View wants to be, given a width of 240 pixels). You can trigger a layout pass using `requestLayout()`, this method is typically called by a View on itself when it believes it can no longer fit within its bounds.

* `WindowManager` Library: You use its `WindowInfoTrackerCallbackAdapter` API in an activity to listen for window configurations/features allowing to detect foldable devices or hinges. in onCreate `windowInfoTracker = new WindowInfoTrackerCallbackAdapter(WindowInfoTracker.getOrCreate(this));`, in onStart `windowInfoTracker.addWindowLayoutInfoListener(this, Runnable::run, layoutStateChangeCallback);`, in onStop `windowInfoTracker.removeWindowLayoutInfoListener(layoutStateChangeCallback);`. The call back added in onStart allows us to pass `WindowLayoutInfo` objects which we then can use to know more info about the device's screen configuration, from this object we can get `DisplayFeature` objects, if one of those objects is of type `FoldingFeature`, then we call `getFeaturePositionInViewRect` which returns a Rect representing the feature’s position inside the given view. Check [this example](https://github.com/android/user-interface-samples/blob/main/WindowManager/app/src/main/java/com/example/windowmanagersample/SplitLayout.kt#L57) to see how all these APIs from the `WindowManager` can be used. `resolveSizeAndState` is a very handy method used to reconcile a view's desired size and state, with constraints imposed by a MeasureSpec. Will take the desired size, unless a different size is imposed by the constraints. The returned value is a compound integer, with the resolved size in the `MEASURED_SIZE_MASK` bits and optionally the bit `MEASURED_STATE_TOO_SMALL` set if the resulting size is smaller than the size the view wants to be. This method was not used in the mentioned example but is usually used inside the `onMeassure` method. WindowManager is now integrated into jetpack's `Navigation`, `SlidingPaneLayout`(is a two-pane layout use in adaptive UI when displayed on large screens), `Preference`(API to create a native look settings screen) as well as `Navigation2` for jetpack compose

* **Theming**: Check material's [theming documentation](https://m2.material.io/develop/android/theming), you can see the shape theming and how you can define shape for specific types of views across the whole app or specifically for some instance of specific views

* **Animating Icons**: You can use lottie, find the animated icons on websites like "Lord Icons" or "Lottie Files" and use Lottie, another option is to animate paths(vector images) individually and manually, another option is to use a tool called rive which has its own android library, all of these methods require you to get a vector image from similar websites to the ones I mentioned previosly. [here](https://www.youtube.com/watch?v=REyOGQiM7zc) is the YT channel that provided this info. Previous 3 solutions work for compose but a solution for views we have two solutions, off course there could be more, the first is using `objectAnimator` in an XML, define its property `propertyName` to `"pathData"`, `valueType` to `pathType`, then the `"valueFrom"` and `"valueTo'` are svg path instructions(`M x,y` to move to an specific coordinate, `L x,y` to draw a line) but if you choose this alternative you have to make sure the paths have the same number of points, actually this will be a requirement in the other alternative also(if you want to create correct icon animations/transitions), you could actually use this solution all the time but there is a catch, it is a time consuming task creating paths that have the same points at design time, and therefore that is why we have the next alternative, in this solution we use a website called "Shape Shifter", we would import two SVGs, if they don't have same quantity of points we can define more points, reorder, delete points. Check [this talk](https://www.youtube.com/watch?v=hLMeIzd4c-g&t=213s) to learn how to use it, basically Shape shifter allows us to create animated icons by defining common points between two shapes/paths/svgs, when the creation process is finished then you can export it to different formats, an `AnimatedVectorDrawable`(AVD)(loading those files at runtime is a little expnsive), also SVG sprite sheet(common format used in the web), also to CSS keyframes, another option the website was going to support is to export to body motion file format, a format used to store motion trajectories of a body model, this options streamlines the process of creating interesting animated icons. Another alternative is use Adobe After Effects but using it just for that(if you're not someone who works with this tool very often) is like riding a space ship just to go to a next door shop. [Here](AnimatedVectorDrawable) is another example that shows how to use `AnimatedVectorDrawbl`. [Here](https://www.youtube.com/watch?v=dzB-1VlHJW8&t=605s) is another great talk about ADV, here we learn this all happens now in the render thread, and that is more efficient to animate root's `alpha` instead of `fillAlpha` or `strokeAlpha` when animating alpha, also is better to animating a group transform is less work than doing path morphing(path morphing is basically what when setting a from value and to value to a path definition, meaning a path defined by the points created with commands like M, L, etc), so if it is pobodyssible just animate a groups property, animating rooth alpha and group transformations is encouraged because both leverages cached  but wouldn't help us actually achive every animation, so in those cases just do whatever you need to to(path mofing, fillAlpha, stokeAlpha), in that talk we also learnt that serious motion deisgners actually use adobe after effects, it seems lottie uses files produces by after effects(a json that is created from defining your animation in adobe after effects) It seems that Lottie lets you have more control on the progress of the animation than AVD, however your decison on which solutin to use should be based on performance, generally AVD is less resource intensive than lottie. Also there is a plugin in after effects called bodymovin that lets you export animations into json in after effects I think this is what the shape shifter creator was asked to do, enable generate an AnimatedVectorDrawable automatically from a bodymoving genrated json file

* **ConstraintLayout**: we have a lot of concepts/APIs that we can leverage using this container. **Chains** lets you spread views across the layout in a defined manner, you can apply bias to those chains to make them align toward an specific edge of the screen. **Ratio** this helps you grow the a dimension, either width or height, depending on either a defined width or height. **Decorators** this is a broad term which is usually used to refer the helpers which we will see next. **ConstraintHelper**s, they inherit from views so they are views in essence and at the same time it is the parent of other concepts that we'll see next, but first, "pure" helpers(directly implementing `ConstraintHelper` abstract class) are  **Barriers**, **Groups**, **Layer**, **Guidelines**, **ReactiveGuide**, **PlaceHolder**, the last three are just helpers in theory because in practice they actually don't subclass `ConstraintHelper`, they actually inherit directly from `View` class, **Barriers** help create some sort of guidline that is dynamic in the sense that it is affected by the views it references, btw, all these components(which inherently are views) could be called virtual layouts/virtual views also eventhough they don't inheret from `VirtualLayout` as other virtual layouts do as we will see later on, but in the meantime we should know that these views help you control the views they reference and at the same time they have the characteristic of keeping the view hierarchy of the constraint layout flat, which means they are declared at the same level of all other views that are not referenced by the helper view which lives inside the constraint layout, this means views that are not referenced by the helper view can reference the views that are referenced by the helper view, we will see why this detail matter when talking about virtual layouts, so barriers are some sort of guideline which position depends on the size and visibility of the views it references and views that live independently from the barrier can reference the barrier which is why this is similar to a guideline and therefore is some sort of dynamic positioned guideline, **Groups** help you control the visibility of a set of children, **Layer** it lets you animate a group of views like when translating their position, scale, rotate, and fade them, **Guidelines** don't inherit from `ConstraintHelper` but inherit from `View` class directly, so in theory is not a helper but it practice it is, it helps you create a reference any view can use to set its position by defining a reference to the guideline in its constraints, it can have an orientation and its position can be set on the axis of the orientation in a fixed place or by defining a percentage(this makes them very dynamic), you should also know that the helpers and virtual views don't actually add any extra views to the constraint layout hierarchy, **ReactiveGuide** this one also is a helper just and theory since it inherits from view directly, and is used to help a constraint layout create responsive UI, meaning it helps adapt constraint layout to different form factors(screen size changes, i.e. when a foldable device is unfolded or folded) and it can be used inside a `MotionLayout`(constraint layout's child class) also, **PlaceHolder** it lets you create some sort of shell which you can fill later on, but that is done programatically, when the time comes to fill the shell you call `placeholder.setContent(R.id.myView)` and the placeholder swallows the passed view, it can be an existing view or a new view, if it is an existing view then you will have to interact with the passed view through a reference of the placeholder, you can combine its use with a transition manager to get animations, but only if the swallowed view was already drawn on screen before it was swallowed. Then we have a concept that borns from helpers, **VirtualLayouts**, basically sometimes you need to add a `LinearLayout` inside a `ConstraintLayout` it would work fine, however this makes the layout hierarchy more complex and means a resource overhead to your app, but also this blocks you from referencing to the views inside a nested layout meaning a direct child from a constraint layout can't reference a view inside a nested layout, so you can not constraint views from one level to views in a different level of the hierarchy, so it is said that virtual views keep your hierarchy flat because all views are declared at the same level and therefore views not referenced by the virtual layout can use referenced views by the virtual layout to control its position by defining a constraint to the referenced views. **Flow** a virtual layout that can subsitute a linear layout that is declared inside a constraint layout, as you can imagine it lets you position a set of views in a linear fashion by referencing them, **CircularFlow** lets you define a view that acts as a center and then a group of views which are circularly positioned around the center, **Grid** lets you position views in a grid form, actually both Flow and Grid virtual layouts could substitute a `FlexBoxLayout`, there is another special case, `MotionPlaceHolder` this is also a virtual view but it is meant to be used in a motion layout in the same was a placeholder is used in a regular constraint layout, remember motion layout is a subclass of constraint layout and therefore is basically another implementation of a constraint layout, this motion place holder enables us to get animations by default in a motion layout, remember I suggested to use transition manager when using placeholder in a regular constraint layout to get animations.Now that we know about helpers and virtual layouts, you should know that you can create your own helpers and virtual layouts too, conceptually all helpers are virtual views, using helpers to create animations in constraint layouts enables you to encapsulate the animation behavior, also makes you capable of applying that animation to a set of views at once, to do this you would use any of the animations APIs we already talked about, view animatiions, or you can use the new property animation(`ValuAnimator`, `ObjectAnimator`, `LayoutTransition`, this last one is also part of the proprety animation system, these layout transitions use animators under the hood so it is part of that system), to implement such a helper you would override `ConstraintHelper` method `updatePostLayout`. There is other type of helpers but they are used in motion layouts, **MotionHelper** inherits from **ConstraintHelper** and **MotionHelperInterface**, so they are views, there is two, **MotionEffect** automatically inserts keyframes for views moving in a given direction, **Carousel** provides a simple recycler like pattern. Based on a series of Transitions and callback to give you the ability to swap views. `MotionLayout` also has somee utility views, **MotionLabel** designed to create complex animated single-line text within a MotionLayout, examples [here](https://github.com/androidx/constraintlayout/wiki/MotionLabel), **MotionButton** is a button that can be easily animated inside motion layout with these views you need to use a `MotionScene`, and also **MotionTelltales** which is some sort of mock view that can help you understand the motion of a view in a motion layout. A different concept, moving away from helpers, **circular constraints** basically is a version of the circular flow helper however this was created before that helper, use these properties `layout_constraintCircle`, `layout_constraintCircleRadius`, `layout_constraintCircleAngle`. There is two utility widgets defined in the constraint layout package, `ImageFilterView` it is a subclass of `ImageView` but this one is designed for efficient image filtering and manipulation. It allows you to adjust properties like saturation, contrast, warmth, and crossfade without writing custom OpenGL or shader code. `ImageFilterButton` is basically a combination of an `ImageButton` and a `ImageFilterView`, the advantage of using an `ImageButton` over just a regular clickable image is that it can be shown as disabled, offer visual feedback when pressed or focused, it offers visual feedback, it supports state-list drawable.

dispatchKeyEvent (KeyEvent event)
Dispatch a key event to the next view on the focus path. This path runs from the top of the view tree down to the currently focused view. If this view has focus, it will dispatch to itself. Otherwise it will dispatch the next node down the focus path. This method also fires any key listeners.

https://medium.com/androiddevelopers/unbundling-the-stable-windowmanager-a5471ff2907 windowlayoutinfo has a property called displayFeatures(FoldingFeature is the only DisplayFeature implementation)

Important subject that can be linked together are custom views and input events(event dispatching), 

Android Layout Manager

Jetpack App Startup

 AutoSize TextView
